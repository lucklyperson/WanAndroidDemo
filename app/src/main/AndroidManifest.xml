<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wu.wanandroiddemo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask" />
        <activity android:name=".ui.article.ArticleContentActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ui.register.RegisterActivity" />
        <activity android:name=".ui.hot.HotTagActivity" />
        <activity
            android:name=".ui.search.SearchActivity"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.articlegroup.ArticleGroupActivity" />
        <activity android:name=".ui.collect.MyCollectActivity" />
        <activity android:name=".ui.aboutus.AboutUsActivity" />
        <activity android:name=".ui.set.SetActivity" />
        <activity android:name=".ui.ads.AdsActivity" />
        <activity android:name=".ui.aboutus.TestActivity"></activity>
    </application>

</manifest>